# demo uses browser IndexedDB. Set live before building a real deployment.
NEXT_PUBLIC_DATA_MODE=demo
# Empty for same-origin admin. Public Pages build uses the public API Worker URL.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_ADMIN_URL=
NEXT_PUBLIC_SITE_URL=
