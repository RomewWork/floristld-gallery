# 验收记录

## 2026-09-10：根网址自动选择语言

- 取消正常访问时的手动进入页，根 HTML 内联脚本在 React 加载前通过 location.replace 跳转。
- 优先顺序：用户主动切换时保存的 zh/en > 浏览器语言列表中第一个支持的语言 > en。使用新的 gallery-preferred-locale 键，避免旧版“每次访问都写入”的 gallery-locale 被误认为手动选择。
- 明确的 /zh/、/en/ 链接不自动切换，也不覆盖手动偏好；查询参数及 hash 保留；存储禁用时照常跳转。完全禁用 JavaScript 时显示 noscript 语言链接。
- 新增桌面/移动尺寸语言入口测试 14 项通过；现有单元/接口测试 63 项通过；类型检查、相关文件 lint、生产构建通过。静态输出另行验证了中文、英文、不支持语言回退，以及屏蔽 Next 客户端脚本和完全禁用 JavaScript 的情况。
- 本轮未部署线上域名，未修改 .env.local 或云端配置；需将最新 out/ 重新部署到现有公共站点。此次改动不增加 Pages Functions 或后端请求。

更新时间：2026-09-09。状态：本地功能与构建验收通过，独立修复复审通过；尚未部署。

## 已有验证证据

- `npm run typecheck`：通过。
- `npm run lint`：通过，未跳过业务源码。
- `node node_modules/vitest/vitest.mjs run`：56 项通过（45 后端、5 演示写入、4 客户端边界、2 上传恢复/取消），含实际 SQLite 执行的接口测试；ImageKit 网络边界使用受控替身，不是真实云联调。
- `npm run test:e2e`：16 项通过（桌面和移动尺寸各 8 项），最终执行退出码 0。
- 覆盖浏览、中英切换、键盘灯箱、触屏滑动/双指缩放、合集创建/刷新/回收/恢复、图片处理上传和发布、图片失败重试、上传末尾刷新失败恢复。
- 大图/双文件上传：输入 PNG >10 MiB，输出各 ≤5,000,000 bytes、长边 ≤3840；两个作品刷新保留；透明图角落 alpha=0。
- 1000 件作品集合：直达第 1000 件作品并保持中英文上下文，首屏不渲染整个合集（网格少于 30 项）。
- `npm run build`：成功导出 10 个静态页面；`npm run admin:build`：成功打包管理资源。
- 管理与公开 Worker 分别执行 `wrangler deploy --dry-run`：均退出 0，没有真实部署。
- `npm run db:local`：0001 和 0002 迁移均成功（首次运行出现 Wrangler 外部日志目录权限警告，迁移本身成功）。
- `node scripts/local-api-smoke.mjs`：在真正的本地 Wrangler/Workers 运行时通过公开读取、本地开发认证、草稿隔离、合集 CRUD、回收/恢复；测试仅删除自己创建的本地空合集，没有触碰远程数据。
- `node scripts/visual-check.mjs`：开发服务及静态生产预览均通过：首页、合集、About、后台共 8 张截图，无页面异常与水平溢出。截图在 `test-results/visual/`，为测试输出，可重新生成；最终截图来自静态输出。

## 已关闭的修复项

- 千张作品中选中项遗漏、演示并发丢写、旧排序覆盖、完成响应丢失重复上传。
- 过期会话同文件核验、确定被拒绝的公开副本清理、未知结果保留清理记录。
- 创建作品重试去重、取消阻止后续重试、页面卸载停止后续队列、刷新失败解锁队列。
- 移动导航断词、图片加载失败反馈、独立管理站返回公共画廊链接。细节见 integration-fix-report.md。

## 尚未测试，不能算上线通过

- Cloudflare 真实部署、D1 远程迁移、Access 两个邮箱验证码收件及备用域名绕过检查。
- ImageKit 真实签名直传、私有图片、公开副本、删除/CDN 缓存以及服务额度耗尽行为。
- 妹妹的真实原画颜色/方向/线稿/渐变人工画质验收；测试噪声图不能代替审美检查。
- 真手机硬件内存与浏览器差异；Playwright 移动尺寸使用 Chromium，并不等于 iPhone Safari 实机。
- 海外与中国大陆运营商网络；本机访问 localhost 不代表跨境速度。

## 真实联调需要

Cloudflare/D1/Access 配置、两位管理员邮箱、ImageKit 免费账户配置、最终公共和管理域名，以及少量有授权的原画样本。密钥只录入本地忽略文件或平台 Secret，不粘贴到源码、截图或公开文档。详见 deployment.md。
